{
  "name": "boe_landing_backend",
  "version": "1.0.0",
  "description": "BeOnEdge landing API. Dependency-free TypeScript service (Node built-in http + crypto) that powers the public landing page: auth, public catalog, admin approvals, and lead capture.",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "start": "node --env-file-if-exists=.env dist/index.js",
    "dev": "npm run build && npm run start"
  },
  "keywords": [
    "beonedge",
    "landing",
    "api",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=20.6"
  },
  "dependencies": {
    "mongodb": "^6.10.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
